# 📊 v5.2 - 可定制表格列与用户体验升级

**发布时间**: 2025年12月12日
**更新类型**: 📊 **用户体验功能增强**

## 🎯 版本概述

v5.2 是一个以用户体验为核心的功能增强版本，引入了**可定制表格列系统**，使用户能够完全控制持股列表和自选列表的显示内容和顺序。这一功能满足了不同用户的个性化需求：投资新手可以选择简洁模式只显示核心信息，而专业投资者可以启用详细模式查看所有数据维度。

在技术层面，v5.2 实现了**元数据驱动的列系统**，将列定义、国际化翻译、排序功能统一集成到单一注册表中，并新增了 `columns.go` 模块（492行）来管理所有列相关的逻辑。这一架构设计为未来的列扩展（如自定义计算列、动态列宽）奠定了坚实基础。

v5.2 完全向后兼容 v5.1，未配置列时自动使用默认设置，确保现有用户平滑升级。

---

## 🎨 核心功能

### 1. **可定制持股列表（Portfolio）列**

**14个可配置列**:

**必需列**（4列，无法移除）:
- `cursor` - 光标选中指示器（►）
- `code` - 股票代码
- `name` - 股票名称
- `price` - 现价

**可选列**（10列，可自由启用/禁用）:
- `prev_close` - 昨收价
- `open` - 开盘价
- `high` - 最高价
- `low` - 最低价
- `cost` - 成本价
- `quantity` - 持股数
- `today_change` - 今日涨幅
- `position_profit` - 持仓盈亏
- `profit_rate` - 盈亏率
- `market_value` - 市值

**配置示例**:
```yaml
display:
  portfolio_columns:
    - cursor
    - code
    - name
    - price
    - position_profit
    - profit_rate
```

### 2. **可定制自选列表（Watchlist）列**

**12个可配置列**:

**必需列**（5列，无法移除）:
- `cursor` - 光标选中指示器（►）
- `tag` - 标签
- `code` - 股票代码
- `name` - 股票名称
- `price` - 现价

**可选列**（7列，可自由启用/禁用）:
- `prev_close` - 昨收价
- `open` - 开盘价
- `high` - 最高价
- `low` - 最低价
- `today_change` - 今日涨幅
- `turnover` - 换手率
- `volume` - 成交量

**配置示例**:
```yaml
display:
  watchlist_columns:
    - cursor
    - tag
    - code
    - name
    - price
    - today_change
    - turnover
```

### 3. **元数据驱动的列系统**

**新增模块**: `columns.go` (492 lines)

v5.2 引入了统一的列注册表系统，将所有列元数据集中管理：

```go
type ColumnMetadata struct {
    ID         ColumnID   // 列ID（如 "price", "cost"）
    I18nKey    string     // 国际化翻译键（如 "col.price"）
    IsRequired bool       // 是否为必需列
    SortField  *SortField // 关联的排序字段（nil表示不可排序）
}
```

**核心优势**:
- **单一真相来源**: 列定义、翻译键、排序关联集中在一处
- **类型安全**: 使用 `ColumnID` 类型防止字符串拼写错误
- **国际化集成**: 每个列自动映射到中英文翻译
- **排序集成**: 列直接关联排序字段，无需额外映射

### 4. **使用场景支持**

**场景一：简洁模式（投资新手）**

只显示核心信息，界面简洁清晰：
```yaml
portfolio_columns: [cursor, code, name, price, position_profit, profit_rate]
watchlist_columns: [cursor, tag, code, name, price, today_change]
```

**场景二：详细模式（专业投资者）**

显示所有可用数据，全面分析：
```yaml
portfolio_columns: [cursor, code, name, prev_close, open, high, low, price, cost, quantity, today_change, position_profit, profit_rate, market_value]
watchlist_columns: [cursor, tag, code, name, price, prev_close, open, high, low, today_change, turnover, volume]
```

**场景三：自定义优先级**

将重要信息放在前面：
```yaml
portfolio_columns:
  - cursor
  - code
  - name
  - price
  - position_profit  # 盈亏信息优先
  - profit_rate
  - cost
  - quantity
  ...
```

---

## 🛠️ 技术实现

### 核心架构变更

#### 1. **新增模块: columns.go**

**文件统计**: 492 lines

**核心函数**:
- `initColumnRegistry()` - 初始化全局列注册表
- `makePortfolioColumnRegistry()` - 构建Portfolio列元数据
- `makeWatchlistColumnRegistry()` - 构建Watchlist列元数据
- `GetPortfolioColumns()` - 获取当前配置的Portfolio列
- `GetWatchlistColumns()` - 获取当前配置的Watchlist列
- `GeneratePortfolioHeader()` - 生成Portfolio表头（含排序指示器）
- `GeneratePortfolioRow()` - 生成Portfolio数据行
- `GenerateWatchlistHeader()` - 生成Watchlist表头
- `GenerateWatchlistRow()` - 生成Watchlist数据行

#### 2. **types.go 扩展**

新增配置字段（12 lines）:
```go
type DisplayConfig struct {
    // ... 现有字段 ...
    PortfolioColumns []string `yaml:"portfolio_columns"` // v5.2新增
    WatchlistColumns []string `yaml:"watchlist_columns"` // v5.2新增
}
```

#### 3. **persistence.go 增强**

新增默认配置逻辑（121 lines）:
- `getDefaultPortfolioColumns()` - 返回默认Portfolio列配置
- `getDefaultWatchlistColumns()` - 返回默认Watchlist列配置
- `ensureRequiredColumns()` - 确保必需列存在
- 配置加载时自动补全缺失的必需列

#### 4. **main.go 简化**

**代码减少**: -233 lines (删除了硬编码的列生成逻辑)

**变更原因**:
- 之前表格行生成逻辑分散在多处，硬编码列顺序
- 现在统一由 `columns.go` 模块处理，main.go 只负责调用
- 简化了 `viewMonitoring()` 和 `viewWatchlistViewing()` 函数

### 国际化更新

#### 5. **i18n/zh.json 新增翻译** (+19 entries)

```json
{
  "col.cursor": "",
  "col.code": "代码",
  "col.name": "名称",
  "col.prev_close": "昨收价",
  "col.open": "开盘",
  "col.high": "最高",
  "col.low": "最低",
  "col.price": "现价",
  "col.cost": "成本价",
  "col.quantity": "持股数",
  "col.today_change": "今日涨幅",
  "col.position_profit": "持仓盈亏",
  "col.profit_rate": "盈亏率",
  "col.market_value": "市值",
  "col.tag": "标签",
  "col.turnover": "换手率",
  "col.volume": "成交量"
}
```

#### 6. **i18n/en.json 新增翻译** (+19 entries)

```json
{
  "col.cursor": "",
  "col.code": "Code",
  "col.name": "Name",
  "col.prev_close": "PrevClose",
  "col.open": "Open",
  "col.high": "High",
  "col.low": "Low",
  "col.price": "Price",
  "col.cost": "Cost",
  "col.quantity": "Quantity",
  "col.today_change": "Today%",
  "col.position_profit": "PositionP&L",
  "col.profit_rate": "P&LRate",
  "col.market_value": "Value",
  "col.tag": "Tag",
  "col.turnover": "Turnover",
  "col.volume": "Volume"
}
```

---

## 📝 详细变更记录

### 提交历史

| Commit Hash | 提交信息 | 时间 |
|-------------|---------|------|
| 5ff8db0 | Support custom display table columns | 2025-12-12 09:07 |

### 文件变更统计

**总体统计**:
- 📝 7个文件修改
- ➕ 766行新增
- ➖ 233行删除
- 🆕 1个新文件创建 (`columns.go`)

**详细变更**:
- `columns.go`: +492/0 lines (🆕 新增)
- `cmd/conf/config_demo.yaml`: +93/0 lines
- `persistence.go`: +121/-0 lines
- `i18n/zh.json`: +19/0 entries
- `i18n/en.json`: +19/0 entries
- `types.go`: +12/-1 lines
- `main.go`: +10/-232 lines (★★★ 大幅简化)

---

## 🔄 与 v5.1 的主要变更

| 方面 | v5.1 | v5.2 | 改进 |
|------|------|------|------|
| **表格列** | 硬编码，固定顺序 | 完全可配置，自定义顺序 | ✨ 灵活性↑↑↑ |
| **用户体验** | 统一显示方式 | 支持简洁/详细/自定义模式 | 🎯 个性化 |
| **列系统架构** | 分散在多处 | 元数据驱动，集中管理 | 🏗️ 可维护性↑ |
| **代码模块数** | 17个 | 18个 | ➕ columns.go |
| **main.go行数** | 3,156行 | 2,923行 | ➖ 233行（简化7.4%） |
| **国际化条目** | ~250条 | ~269条 | ➕ 19条列翻译 |
| **配置文件大小** | 150行 | 243行 | ➕ 93行（列配置） |
| **向后兼容** | N/A | ✅ 完全兼容 | 🔒 平滑升级 |

---

## 🚀 升级指南

### 从 v5.1 升级的步骤

**步骤 1: 拉取最新代码**
```bash
cd /path/to/stock-go
git pull origin master
go mod download
```

**步骤 2: 重新编译应用**
```bash
go build -o cmd/stock-monitor
```

**步骤 3: (可选) 自定义列配置**

编辑 `cmd/conf/config.yml`，添加列配置：
```yaml
display:
  # 持股列表列配置
  portfolio_columns:
    - cursor
    - code
    - name
    - price
    - position_profit
    - profit_rate

  # 自选列表列配置
  watchlist_columns:
    - cursor
    - tag
    - code
    - name
    - price
    - today_change
```

**步骤 4: 重启应用查看效果**
```bash
./cmd/stock-monitor
```

### 兼容性说明

✅ **完全向后兼容**

- ✅ **配置文件**: 旧版 config.yml 继续可用，不配置列时使用默认设置
- ✅ **数据文件**: portfolio.json 和 watchlist.json 无需修改
- ✅ **现有功能**: 所有 v5.1 功能继续正常工作
- ✅ **必需列保护**: 即使配置错误，必需列也会自动补全

---

## 📋 配置指南

### 配置位置

`cmd/conf/config.yml` - 在 `display` 节点下添加

### 配置语法

```yaml
display:
  portfolio_columns:   # 持股列表列配置
    - cursor           # 每个列ID占一行
    - code
    - name
    ...

  watchlist_columns:   # 自选列表列配置
    - cursor
    - tag
    ...
```

### 配置规则

1. **必需列自动补全**: 即使配置中缺失必需列，系统也会自动添加
2. **无效列忽略**: 配置中的无效列ID会被静默忽略（debug模式下记录警告）
3. **列顺序**: 列按配置文件中的顺序从左到右显示
4. **注释隐藏**: 使用 `#` 注释掉某一行即可隐藏该列

### 推荐配置模式

**模式 1: 最简模式（适合小屏幕）**
```yaml
portfolio_columns: [cursor, code, name, price, position_profit]
watchlist_columns: [cursor, tag, code, name, price]
```

**模式 2: 核心模式（推荐）**
```yaml
portfolio_columns: [cursor, code, name, price, cost, quantity, position_profit, profit_rate]
watchlist_columns: [cursor, tag, code, name, price, today_change, turnover]
```

**模式 3: 完整模式（专业投资者）**
```yaml
portfolio_columns: [cursor, code, name, prev_close, open, high, low, price, cost, quantity, today_change, position_profit, profit_rate, market_value]
watchlist_columns: [cursor, tag, code, name, price, prev_close, open, high, low, today_change, turnover, volume]
```

---

## 🧪 测试验证

### 测试场景

**场景 1: 默认配置（向后兼容测试）**
- 配置: 不添加 `portfolio_columns` 和 `watchlist_columns`
- 预期: 应用使用默认列配置，所有列正常显示
- 结果: ✅ PASS - 与v5.1相同的显示效果

**场景 2: 简洁模式**
- 配置: 只配置6个核心列
- 预期: 界面只显示配置的6列，表格宽度减小
- 结果: ✅ PASS - 界面简洁，适合小屏幕

**场景 3: 自定义顺序**
- 配置: 将 `position_profit` 和 `profit_rate` 放在前面
- 预期: 盈亏列显示在靠前位置
- 结果: ✅ PASS - 列顺序符合配置

**场景 4: 必需列保护**
- 配置: 故意不配置 `code` 列
- 预期: 系统自动补全 `code` 列
- 结果: ✅ PASS - 必需列自动添加，应用正常运行

**场景 5: 无效列处理**
- 配置: 添加不存在的列ID `invalid_column`
- 预期: 无效列被忽略，其他列正常显示
- 结果: ✅ PASS - 静默忽略无效列

### 已知问题

- 无已知问题 ✅

---

## 📊 代码质量

### 代码统计 (v5.2)

| 模块 | 行数 | 变化 | 职责 |
|------|------|------|------|
| main.go | 2,923 | -233行 | 状态机、事件处理（简化7.4%） |
| api.go | 1,271 | 无变化 | 外部API集成 |
| intraday.go | 1,394 | 无变化 | 盘中数据采集 |
| intraday_chart.go | 898 | 无变化 | 图表可视化 |
| watchlist.go | 508 | 无变化 | 观察列表管理 |
| columns.go | 492 | +492行 | 列元数据管理（🆕新增） |
| types.go | 286 | +12行 | 数据结构 |
| persistence.go | 292 | +121行 | 数据持久化（列配置加载） |
| **总计** | **~15,200** | **+533行** | **18个模块** |

### 架构改进

**优势**:
- ✅ **单一职责**: `columns.go` 专注于列管理，main.go 专注于业务逻辑
- ✅ **可扩展性**: 未来添加新列只需修改 `columns.go` 和配置
- ✅ **可维护性**: 列定义集中，避免代码重复
- ✅ **类型安全**: 使用 `ColumnID` 类型防止拼写错误

---

## 🎯 后续版本规划

### v5.3 计划 (预计 2025年Q1)

**用户体验增强**
- [ ] 列宽配置支持
- [ ] 表格主题切换（light/dark/custom）
- [ ] 列拖拽排序（交互式配置）

**数据可视化**
- [ ] 自定义计算列
- [ ] 列数据汇总行（求和、平均值等）
- [ ] 导出表格为CSV/Excel

---

## 📝 更新日志

### v5.2.0 (2025-12-12)

**📊 核心功能**:
- ✅ **可定制表格列**: Portfolio 14列、Watchlist 12列完全可配置
- ✅ **元数据驱动系统**: 新增 `columns.go` 模块（492行）
- ✅ **使用场景支持**: 简洁模式、详细模式、自定义优先级模式
- ✅ **配置文件增强**: config_demo.yaml 新增93行配置示例
- ✅ **国际化完善**: 新增19条列翻译（中英文）

**🛠️ 架构改进**:
- ✅ **main.go简化**: -233行（7.4%代码减少）
- ✅ **persistence.go增强**: +121行（默认配置加载）
- ✅ **完全向后兼容**: 无breaking changes

---

**下一步**: [查看 v5.1 版本文档](v5.1.md) · [返回版本索引](README.md)
